# ============================================================
# 1-kong-api-gateway-global.yaml  →  Istio IngressGateway resource
# Replaces: Global Kong Gateway + global-api-gateway-ns namespace
# Purpose:  Istio Gateway binds to the istio-ingressgateway pod
#           (the single LoadBalancer) and accepts traffic on
#           port 80/443 for finance.mhnbank.xyz.
#           VirtualService (file 2-downstream-proxy-services.yaml)
#           handles path-based routing into the mesh.
# ============================================================
---
apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: global-istio-gateway
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway
  servers:
    # ── API Gateway (finance.mhnbank.xyz) ────────────────────
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "finance.mhnbank.xyz"
    - port:
        number: 443
        name: https
        protocol: HTTPS
      tls:
        mode: SIMPLE
        credentialName: finance-mhnbank-xyz-tls   # K8s Secret with TLS cert
      hosts:
        - "finance.mhnbank.xyz"
    # ── Keycloak Identity Provider (auth.mhnbank.xyz) ────────
    # Keycloak's login page and OIDC endpoints must be reachable
    # by browsers during the OAuth2 Authorization Code flow
    # (steps 5-6 in the 21-step auth flow).
    - port:
        number: 80
        name: keycloak-http
        protocol: HTTP
      hosts:
        - "auth.mhnbank.xyz"
---